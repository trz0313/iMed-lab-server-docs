# Hardware Dashboard

Dashboard URL:

```text
http://128.100.23.241/gpu
```

The dashboard requires ECE VPN access.

It shows:

- GPU utilization
- GPU memory usage
- GPU memory used
- GPU temperature
- CPU usage
- RAM usage
- disk usage
- GPU processes by user, when active GPU processes are present

The dashboard updates approximately once per minute.

# VS Code Remote SSH

1. Install the VS Code **Remote - SSH** extension.
2. Add the workstation to your local SSH config.
3. Connect to `imed-ws1` or `imed-ws2`.
4. Open your own home folder or project folder.

Recommended working folders:

```text
~/code
~/data
~/scratch
```

Do not open or modify other users' private folders.
